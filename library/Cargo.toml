[package]
name = "bluebrick-lmsh1"
version = "0.1.0"
edition = "2024"

[dependencies]
bluebrick = { path = "../../loader/rust-bindings" }
bluebrick-proc-macros = { path = "../../loader/proc_macros" }
msgbox = "0.7.0"

[lib]
crate-type = ["cdylib", "rlib"]
